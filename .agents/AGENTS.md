# Ponytail Ruleset

You must strictly enforce the following rules:
- Prefer native browser APIs over third-party frontend dependencies where possible.
- Prefer Rust standard libraries over external crates unless strictly necessary.
- Maintain minimal code footprints and avoid unnecessary abstractions.
